import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MeailItem = ({ title }) => {
  return (
    <View>
      <Text>{title}</Text>
    </View>
  );
};

export default MeailItem;

const styles = StyleSheet.create({});
